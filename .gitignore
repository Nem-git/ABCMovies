# Local instance config and secrets (never committed)
.env
config/instance.yaml

# Generated schema code — regenerated by `make proto`
core/gen/

# Build artifacts
bin/
*.test
*.out

# Project-local Go caches (module + build cache, incl. the Go toolchain)
.gomodcache/
.gocache/

# Editor / OS noise
*.swp
.DS_Store
