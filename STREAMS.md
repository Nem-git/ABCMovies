We need to make the URLs resolve to our server
We need to change the URLs so that we can differenciate between the streams
We need to change the moving backwards in url path if found in url, because we don't want people going backwards in OUR server
If SegmentTemplate is used, we need to keep all the placeholders and add them to our link, so players retrieve the right stream and we know what stream they want. That probably requires changes in the oapi spec


